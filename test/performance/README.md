# Performance fixtures

Cases record inputs, toolchain, hardware, units, variance, compiler time, C
compile time, runtime, allocation, code size, and reviewed budgets. See
[`docs/testing.md`](../../docs/testing.md).

This lane is contract-only at M0. No performance claim is implied.
